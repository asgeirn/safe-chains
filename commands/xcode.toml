# Skipped CommandDef: xcodebuild, plutil, xcode-select, xcodegen, tuist,
#   pod, swiftlint, periphery, agvtool, simctl
# Skipped custom handlers: xcrun, pkgutil, lipo, codesign, spctl, swiftformat

[[command]]
name = "xcbeautify"
url = "https://github.com/cpisciotta/xcbeautify"
level = "Inert"
bare = true
standalone = ["--help", "--is-ci", "--quiet", "--quieter", "--version", "-V", "-h", "-q"]
valued = ["--renderer"]
